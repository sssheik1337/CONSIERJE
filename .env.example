# Пример файла окружения для бота

BOT_TOKEN=
DB_PATH=./concierge.sqlite3
TINKOFF_NOTIFY_URL=https://your-ngrok-id.ngrok-free.app/tbank_notify

# Интервал тестовой проверки подписок (в минутах).
# Если задан, планировщик будет запускать проверку подписок каждые N минут
# вместо ночного расписания. Используется только для тестовой среды.
TEST_RENEW_INTERVAL_MINUTES=0
